{
  "name": "bookshop",
  "version": "1.0.0",
  "description": "Kitap Katalogu - CAP TypeScript",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4"
  },
  "devDependencies": {
    "@sap/cds-dk": "^8",
    "typescript": "^5",
    "@types/node": "^20"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build && tsc"
  }
}
